<% content_for :page_title, "Editar Proveedor" %>

<div class="max-w-2xl mx-auto space-y-5">
  <div class="flex items-center justify-between">
    <h1 class="text-xl font-bold text-stone-800">Editar Proveedor</h1>
    <%= link_to "← Volver", suppliers_path, class: "text-sm text-stone-500 hover:text-stone-700 font-medium", data: { turbo_frame: "_top" } %>
  </div>
  <%= render "form", supplier: @supplier %>
</div>
